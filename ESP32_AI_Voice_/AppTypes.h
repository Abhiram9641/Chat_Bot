// AppTypes.h — intentionally empty.
// ErrSource and AppState are defined directly in ESP32_AI_Voice_v7.ino
// to avoid duplicate-symbol errors from Arduino's auto-compilation of
// all .h files in the sketch folder.
#pragma once
